<div class="page-header">
  <h1>Fil d'actualité — Tous les immeubles</h1>
</div>

<div class="space-y-4">
  <% @publications.group_by(&:building).each do |building, pubs| %>
    <div class="card">
      <div class="card-header">
        <%= link_to building.name, building, class: "card-title no-underline text-inherit hover:text-primary" %>
        <span class="text-xs text-muted"><%= pubs.size %> publication<%= "s" if pubs.size > 1 %></span>
      </div>
      <div class="card-content">
        <% pubs.each do |p| %>
          <div class="<%= 'mb-4 pb-4 border-b' unless p == pubs.last %>">
            <div class="flex gap-3 mb-2">
              <div class="pub-avatar"><%= p.tenant.first_name[0] %><%= p.tenant.last_name[0] %></div>
              <div>
                <div class="font-medium text-sm"><%= link_to p.tenant.full_name, tenant_path(p.tenant), class: "text-link" %></div>
                <div class="text-xs text-muted"><%= l(p.created_at, format: :long) %></div>
              </div>
            </div>
            <div style="font-size: 0.9375rem; line-height: 1.6;"><%= simple_format(p.content) %></div>
            <div class="flex items-center gap-3 mt-2 pt-2" style="border-top: 1px solid var(--border);">
              <%= button_to like_publication_path(p), method: :post, class: "btn btn-ghost btn-sm", style: "gap: 0.375rem;" do %>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="<%= p.liked_by?(current_user) ? 'currentColor' : 'none' %>" stroke="currentColor" stroke-width="2"><path d="M14 9V5a3 3 0 0 0-3-3l-4 9v11h11.28a2 2 0 0 0 2-1.7l1.38-9a2 2 0 0 0-2-3.3H14zM7 22H4a2 2 0 0 1-2-2v-7a2 2 0 0 1 2-2h3"/></svg>
                <%= p.likes_count %>
              <% end %>
              <%= link_to publication_path(p), class: "btn btn-ghost btn-sm", style: "gap: 0.375rem;" do %>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
                <%= p.comments_count %>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

  <% if @publications.empty? %>
    <div class="card">
      <div class="empty-state">
        <div class="empty-state-icon">📰</div>
        <h3>Aucune publication</h3>
        <p>Aucun locataire n'a encore publié de message.</p>
      </div>
    </div>
  <% end %>
</div>
